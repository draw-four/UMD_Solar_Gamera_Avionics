# setup/run

this python project is managed using uv. you can download uv [here](https://docs.astral.sh/uv/getting-started/installation/#pypi). It can either be installed directly using your platforms package manager, or through pyPI/pip.

from there, you can ``uv sync`` to install any dependencies to the programs virtual environment, and then ``uv run main.py``.
